image: node:14.16.0-buster

stages:
  - test
  - deploy

test:
  stage: test
  script:
    - cd media-service
    - npm install --force
    - npm run test

deploy:
  stage: deploy
  script: 
    - cd media-service
    - cp $ENV_FILE .env
    - docker compose down
    - docker rmi media-service_media-service
    - docker build -t media-service .