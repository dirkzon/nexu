apiVersion: extensions/v1beta1
kind: Ingress
metadata:
    name: "nexucluster-f342"
    labels:
        app: "nexucluster-f342"
    annotations:
        kubernetes.io/ingress.class: addon-http-application-routing
spec:
    rules:
        - host: default-nexucluster-f342.5521d5a114fc4eb38320.uksouth.aksapp.io
          http:
            paths:
            - path: /
              backend:
                serviceName: "nexucluster-f342"
                servicePort: 4000