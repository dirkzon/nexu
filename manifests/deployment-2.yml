apiVersion : apps/v1
kind: Deployment
metadata:
  name: "nexucluster-24bb"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: "nexucluster-24bb"
  template:
    metadata:
      labels:
        app: "nexucluster-24bb"
    spec:
      containers:
        - name: "nexucluster-24bb"
          image: "s6nexu.azurecr.io/nexucluster"
          ports:
          - containerPort: 7000