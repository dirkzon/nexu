apiVersion : apps/v1
kind: Deployment
metadata:
  name: "nexucluster-5bd5"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: "nexucluster-5bd5"
  template:
    metadata:
      labels:
        app: "nexucluster-5bd5"
    spec:
      containers:
        - name: "nexucluster-5bd5"
          image: "s6nexu.azurecr.io/nexucluster"
          ports:
          - containerPort: 3000