apiVersion: extensions/v1beta1
kind: Ingress
metadata:
    name: "nexucluster-5ac0"
    labels:
        app: "nexucluster-5ac0"
    annotations:
        kubernetes.io/ingress.class: addon-http-application-routing
spec:
    rules:
        - host: default-nexucluster-5ac0.5521d5a114fc4eb38320.uksouth.aksapp.io
          http:
            paths:
            - path: /
              backend:
                serviceName: "nexucluster-5ac0"
                servicePort: 2000